#!{ shared_url: git@github.secureserver.net:PC/cicd-shared.git, organization: PC }
node:
  version: 14.16.0
  npm_version: 6.14.11

autobuild: false

workflows:
  master:
    build:
      tasks:
        - nvm: npm i
        - shell: time tar czf /tmp/artifact.tgz node_modules
